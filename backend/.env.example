# Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=budgeting
POSTGRES_USER=budgeting
POSTGRES_PASSWORD=budgeting

# Server Configuration
PORT=3001
NODE_ENV=development

# Embeddings run in-process by default (all-MiniLM-L6-v2 via @huggingface/transformers),
# auto-downloaded on first run — no external embedding/LLM endpoint required.
